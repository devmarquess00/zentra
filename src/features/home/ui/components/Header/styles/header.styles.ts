import { StyleSheet } from "react-native";

export const headerStyles = StyleSheet.create({
  header: {
    backgroundColor: "#5249EB",
    height: 220,
    width: "auto",
    paddingHorizontal: 28,
    paddingTop: 52,
  },

  headerContent: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
  },
});
